logzio:
  metrics:
    registry:
      mock: true

spring:
  cloud:
    stream:
      bindings:
        messageEvent-out-0:
          destination: message.created
message-generator:
  client:
    url: localhost:9561